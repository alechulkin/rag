package com.company.rag.shared.model;

/**
 * Workspace / tenant classification tier ({@code standard} / {@code restricted} /
 * {@code strict}).
 */
public enum Classification {
    STANDARD,
    RESTRICTED,
    STRICT
}
