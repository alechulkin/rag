# OpenSpec main specs

Capability specs land here when an OpenSpec change is **archived and synced**
(`openspec-archive-change` / `openspec-sync-specs`).

Until the first archive, this directory intentionally holds no capability
`spec.md` files — active work lives under `openspec/changes/<slice>/specs/`.

Do not invent main specs ahead of archive sync.
